// function deepConvertToNums(object) {
//     for (const key in object) {
//         if (object.hasOwnProperty(key)) {
//             const element = object[key];
//             if(typeof element == "string"){
//                 if(!_.isUndefined(_.toNumber(element))){
//                     object[key] = _.toNumber(element);
//                     continue;
//                 }
//             } else if(typeof element == "object"){

//             }
//         }
//     }
// }

// export {deepConvertToNums};